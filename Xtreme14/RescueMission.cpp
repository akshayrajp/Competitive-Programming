#include <iostream>
using namespace std;
int main()
{
    int n, i, d;
    cin >> n;
    int s[n];
    for(i = 0; i < n; i++)
        cin >> s[i];
    cin >> d;
    long long int li, vi, ri;
    while(d--)
    {
        cin >> li >> ri >> vi;
    }