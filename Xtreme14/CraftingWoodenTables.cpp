#include <bits/stdc++.h>
using namespace std;
int main()
{
    unsigned long long int c, n, p, w;
    cin >> c >> n >> p >> w;
    unsigned long long int pockets[n];
    
    if(w < c)
    {
        cout << 0;
        return 0;
    }

    return 0;
}