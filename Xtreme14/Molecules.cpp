#include <iostream>
using namespace std;
int main()
{
    int t; 
    long long int c, h, o;
    long long cost_h2o, cost_co2, cost_glucose;
    cin >> t;
    while(t--)
    {
        cin >> c >> h >> o;
        // First, let's see what it takes to make glucose
        if(h != 0 && c != 0 && o != 0)
            cost_glucose =    
    }
}